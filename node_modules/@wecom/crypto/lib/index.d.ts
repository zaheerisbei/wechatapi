/// <reference types="node" />
/**
 * 获取签名
 *
 * @param token     Token
 * @param timestamp 时间戳
 * @param nonce     随机串
 * @param encrypt   密文
 */
export declare function getSignature(token: string, timestamp: string | number, nonce: string | number, encrypt: string): string;
/**
 * 解密
 *
 * @param encodingAESKey EncodingAESKey
 * @param encrypt        密文
 */
export declare function decrypt(encodingAESKey: string, encrypt: string): {
    message: string;
    id: string;
    random: Buffer;
};
/**
 * 加密
 *
 * @param encodingAESKey EncodingAESKey
 * @param message        消息文本
 * @param id             ID
 * @param random         随机串
 */
export declare function encrypt(encodingAESKey: string, message: string, id: string, random?: Buffer): string;
export interface GetJsApiSignatureOptions {
    /** 当前页面的 URL */
    url: string;
    /** 用于签名的 jsapi ticket */
    ticket: string;
    /** 生成签名的随机串 */
    nonceStr?: string;
    /** 生成签名的时间戳 */
    timestamp?: number | string;
}
/**
 * 获取 JSAPI 签名
 *
 * @param options 签名相关参数
 */
export declare function getJsApiSignature(options: GetJsApiSignatureOptions): {
    timestamp: string | number;
    nonceStr: string;
    signature: string;
};
